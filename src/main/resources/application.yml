server:
 port: 8080

spring:
 jpa:
  show-sql: true
  hibernate:
   ddl-auto: update
 datasource:
  url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/accounts
  username: root
  password: root
  driver-class-name: com.mysql.cj.jdbc.Driver
